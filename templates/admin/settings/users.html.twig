{% extends 'base.html.twig' %}
{% block title %}User Settings{% endblock %}
{% block body %}
    <h1>Manage Users</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Username</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.username }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.roles|join(", ") }}</td>
                    <td>To Follow</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
